Now that the frontend UI is ready, let's connect it to a database backend. I want to use Supabase for authentication and data storage. Please write the database schema for three tables: users, jobs, and contacts based on standard relationships (one user has many jobs; one job has many contacts).
Provide the SQL migration script to set up these tables, including Row-Level Security (RLS) so users can only see their own data. Then, show me how to update the React frontend to fetch, create, update (specifically moving a card to a different status column), and delete jobs using the Supabase JS client.  